#include "./Directory.hpp"

int main(void)
{
    Directory dr;
    std::string input;

    while (true)
    {
        std::cout << "type 1.ADD 2.SEARCH 3.EXIT : ";
        std::getline(std::cin, input);
    }
    return (-1);
}